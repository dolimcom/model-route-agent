spring:
  application:
    name: model-route-agent
  config:
    import:
      # Local provider profiles only. Do not load shared models.yml at runtime.
      - classpath:models_local.yml
      - optional:classpath:router.yml

server:
  port: 8080
